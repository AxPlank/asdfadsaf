<section class="p-3">
    <div class="p-3 bg-primary-subtle rounded text-primary">
        <h1 class="p-3"><%= table %></h1>
    </div>
    <div class="p-3">
        <%- include('../search') %>
    </div>
    <div class="p-3">
        <div class="p-3">
            <form method="post" action="/admin/personal_info/delete">
                <div class="p-3 form-check">
                    <input type="checkbox" id="allcheck" name="allcheck" value="allinstance" onclick="allclick(this)" class="form-check-input">
                    <label for="allcheck" class="form-check-label">전체 선택</label>
                    <input type="submit" value="삭제" class="btn btn-sm btn-primary">
                </div>
                <div class="p-3">
                    <table class="table">
                        <thead>
                            <tr>
                                <td>#</td>
                                <td>ID</td>
                                <td>닉네임</td>
                                <td>인증</td>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (let i = 0; i < instances.length; i++) { %>
                                <% if (instances[i].authority !== 'admin') { %>
                                <tr>
                                    <td><input type="checkbox" id="instance" name="instance" value="<%= instances[i].personal_id %>" class="form-check-input border border-1 border-primary"></td>
                                    <td><a href="/admin/personal_info/<%= instances[i].personal_id %>"><%= instances[i].user_id %></a></td>
                                    <td><%= instances[i].nickname %></td>
                                    <td><%= instances[i].authentication %></td>
                                </tr>
                                <% } %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </form>
        </div>
    </div>
</section>
html
    head
        link(rel="shortcut icon", href="/favicon.ico")
        meta(charset='utf-8')
    body 
        h1 Image

        each image in image_list
            div
                a(href=`/list/${image.id}`)=image.image_url 
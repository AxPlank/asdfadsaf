html 
    head 
        meta(charset='utf-8')
    body 
        ul 
            each league in leagues
                li 
                    a(href=`/table/${league.league.replace(/ /g, '_')}`)=league.league